(defwidget ram []
  (box :class "ram"
       :orientation "horizontal"
       :space-evenly false
       :spacing 0
       :halign "start"
       :tooltip "RAM"
    (label :text " ")
    (label :text {round((EWW_RAM.used_mem_perc) * 1, 0)})
  )
)
